{
  "name": "cfw-iframe-server",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@sentry/cloudflare": "^8.51.0",
    "hono": "^4.11.1"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}